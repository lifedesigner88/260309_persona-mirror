{
  "name": "backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/backend",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash -lc 'until (echo > /dev/tcp/127.0.0.1/5432) >/dev/null 2>&1; do echo \"waiting for db:5432\"; sleep 1; done; uv run --project apps/backend uvicorn main:app --reload --host 0.0.0.0 --port 8000 --app-dir apps/backend --env-file .env'"
      }
    }
  }
}
