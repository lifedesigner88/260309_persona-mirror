{
  "name": "frontend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/frontend/src",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash -lc 'until (echo > /dev/tcp/127.0.0.1/8000) >/dev/null 2>&1; do echo \"waiting for backend:8000\"; sleep 1; done; pnpm --dir apps/frontend dev'"
      }
    }
  }
}
